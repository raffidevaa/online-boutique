--- main.go
+++ main.go
@@ -238,0 +239,3 @@
+	if os.Getenv("RESEARCH_FAULT_TRIGGER") == "missing_exception_handler" {
+		err = fmt.Errorf("faulty image: simulated dependency failure")
+	}
@@ -240 +243 @@
-		return nil, status.Errorf(codes.Internal, err.Error())
+		panic(fmt.Sprintf("faulty image: unhandled checkout dependency error: %v", err))
